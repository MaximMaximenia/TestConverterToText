/* ============================================================
   НАСТРОЙКИ САЙТА — редактируй только этот файл
   ============================================================

   1) ДАТА И ВРЕМЯ ВЕБИНАРА
      Впиши дату в формате "ГГГГ-ММ-ДДTЧЧ:ММ:00" по МОСКОВСКОМУ
      времени. Таймер наверху сайта сам посчитает, сколько дней/
      часов/минут/секунд осталось.

      Пример: вебинар 25 сентября 2026 года в 19:00 по Москве →
      webinarDate: "2026-09-25T19:00:00+03:00"

   2) ЦЕНА
      priceOld  — обычная цена вебинара (зачёркнутая)
      priceNew  — цена сейчас (пока идёт бесплатный период)
      freeUntilText — текст про то, сколько ещё дней бесплатно

   3) ВИДЕО
      videoSrc  — путь к файлу видео (положи файл в папку assets/
                  и укажи его имя здесь, например "assets/webinar.mp4")
      videoPoster — картинка-заставка, которая показывается до
                  нажатия на плей (можно оставить свою фотографию)

   4) АНАЛИТИКА И ЦЕЛИ

      --- Яндекс.Метрика (основное) ---
      yandexMetrikaId — номер счётчика (просто цифры, без "ym").
      Взять тут: metrika.yandex.ru → счётчик → Настройка → номер
      наверху страницы, например 12345678.

      Как только впишешь номер, сайт сам начнёт отправлять в Метрику
      такие цели (JS-события, ym(ID,'reachGoal', '...')):
        • video_25 / video_50 / video_75 / video_95 — досмотр видео
          до 25/50/75/95% его длины. Считается по факту проигранных
          секунд (currentTime/duration), а НЕ по таймеру — поэтому
          не зависит от скорости интернета у зрителя: если у кого-то
          видео грузится медленно, цель просто сработает позже, а не
          "потеряется".
        • lead_submit — реальная отправка формы записи.

      Дальше в самой Метрике: Настройка счётчика → Цели → Добавить цель →
      тип "JavaScript-событие" → в поле "Идентификатор цели" вписываешь
      ИМЕННО ТУ строку, что в списке выше (video_50, lead_submit и т.д.).
      После этого цель появится в отчётах и её можно использовать для
      Яндекс.Директа (например, откручивать рекламу на тех, кто досмотрел
      до 50%, но не оставил заявку).

      Защита от накрутки/ботов: у Метрики уже есть встроенный фильтр
      ботов и накруток (робоперсонажи не попадают в статистику и в цели
      по умолчанию). Дополнительно форма записи проверяет скрытое поле-
      ловушку — его видят только боты, не люди; если оно заполнено,
      цель lead_submit не отправляется и заявка тихо игнорируется.

      --- Google Analytics 4 (опционально, если нужен вдобавок) ---
      gaMeasurementId — ID вида "G-XXXXXXXXXX" (Админ → Потоки данных).
      Если оставить пустым — просто не подключается, ошибок не будет.
      Шлёт те же события (video_progress, generate_lead) в GA4.

   После правки файла просто обнови страницу в браузере —
   больше ничего трогать не нужно.
   ============================================================ */

window.SITE_CONFIG = {
  // Дата и время начала вебинара
  webinarDate: "2026-09-21T19:00:00+03:00",

  // Цены
  priceOld: "4 990 ₽",
  priceNew: "Сегодня бесплатно",
  freeUntilText: "Только на этом запуске — дальше по полной цене",

  // Видео на главном экране
  videoSrc: "assets/webinar-preview.mp4",
  videoPoster: "https://images.unsplash.com/photo-1552664730-d307ca884978?auto=format&fit=crop&w=1400&q=80",

  // Имя эксперта
  expertName: "Мейнард Скринда",

  // Ссылка/действие формы заявки (пока просто показывает подтверждение;
  // когда будет CRM/бот — впиши сюда URL для отправки формы)
  formEndpoint: "",

  // Яндекс.Метрика — см. пункт 4 выше. Оставь пустым, чтобы не подключать.
  yandexMetrikaId: "",

  // Google Analytics 4 (см. пункт 4 выше). Оставь пустым, чтобы не подключать.
  gaMeasurementId: ""
};
